# Copy to .env (gitignored) and fill in values. Never commit real keys.
# FRED macro data (required by src/config.py for the data pipeline)
FRED_API_KEY=
# EODHD market-data WebSocket + delayed-quote REST (api/stream.py live layer).
# Server-side only — the token is never sent to the browser.
EODHD_API_TOKEN=
