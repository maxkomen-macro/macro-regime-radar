# Lean dependency set for the news-only refresh mode (2026-09-06):
# src/analytics/news.py + src/analytics/perplexity.py + src/db_helpers.py.
# Everything else in requirements.txt (pandas, scikit-learn, prophet,
# streamlit…) is not imported on this path.
requests>=2.31.0
feedparser>=6.0
